# Changelog

## [0.1.0] - 2026-02-28
- 上部タブで `Re:Earth`, `Googleマップ`, `BOX` を切り替え可能に実装
- タブ切替時に iframe の状態が保持されるように、表示/非表示を CSS の `display` で制御
- 初期バージョン `v0.1.0` をタグ付けしてリリース

## [0.2.0] - 2026-03-01
- 左パネルの操作領域を改良（Backlog ボタンは一時除去）
- 右パネルの `設定` タブを追加し、`Re:Earth` / `BOX` の表示 URL を編集・プレビュー・保存できるようにした
- `BOX` のデフォルト表示 URL を埋め込み URL に更新
- URL 状態を `src/PortalContext.js` に移動し、`localStorage` による永続化（プレビュー/保存/初期化機能）を実装
- iframe をアクティブタブでのみ読み込む遅延ロードを追加（パフォーマンス向上）
- ドキュメントに設計方針を追加（README.md）